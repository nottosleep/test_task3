import React from 'react';
import { Routes, Route } from 'react-router-dom';

function RootRouter() {
    return (
        <Routes>
            <Route path="/" />
        </Routes>
    );
}

export default RootRouter;
